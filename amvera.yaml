meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 17

build:
#  artifacts:
#    "build/libs/*.jar": /
  args: -x test

run:
  jarName: library-app-0.0.1-SNAPSHOT.jar
  persistentMount: /data
  containerPort: 8080
