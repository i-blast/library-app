meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 17

build:
#  artifacts:
#    "build/libs/*.jar": /
  args: -Dskip.tests

run:
  jarName: library-app-0.0.1-SNAPSHOT.jar
  persistentMount: /data
  containerPort: 8080
